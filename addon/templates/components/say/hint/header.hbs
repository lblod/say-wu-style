<div class="modal-dialog__header">
  <div class="grid">
    <div class="col--10-12">
      <p>
        <strong>{{yield}}</strong>
      </p>
    </div>
    {{#if @closeHint}}
      <div class="col--2-12 u-align-right">
        <a class="link--icon--close" href="#" {{action @closeHint}}>
          <span class="u-visually-hidden">Venster sluiten</span>
        </a>
      </div>
    {{/if}}
  </div>
</div>
